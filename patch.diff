*** Update File: tools/PostProcessCampaignMerges.ps1
@@
-            $table = $shape.Table
-            $rowCount = $table.Rows.Count
-            if ($rowCount -le 1) { continue }
+            $table = $shape.Table
+            $rowCount = $table.Rows.Count
+            if ($rowCount -le 1) { continue }
+
+            # Pre-pass: undo existing merges so the script remains idempotent.
+            for ($rowIdx = 2; $rowIdx -le $rowCount; $rowIdx++) {
+                try {
+                    $cell = $table.Cell($rowIdx, 1)
+                } catch {
+                    Write-Verbose ("Unable to access cell on slide {0} row {1}: {2}" -f $slide.SlideIndex, $rowIdx, $_)
+                    continue
+                }
+
+                if ($null -eq $cell) { continue }
+
+                $span = 1
+                $scan = $rowIdx + 1
+                while ($scan -le $rowCount) {
+                    try {
+                        $nextCell = $table.Cell($scan, 1)
+                    } catch {
+                        break
+                    }
+
+                    if (-not [object]::ReferenceEquals($cell.Shape, $nextCell.Shape)) { break }
+                    $span += 1
+                    $scan += 1
+                }
+
+                if ($span -gt 1) {
+                    try {
+                        $cell.Split($span, 1) | Out-Null
+                    } catch {
+                        Write-Verbose ("Unable to split campaign span on slide {0} starting row {1}: {2}" -f $slide.SlideIndex, $rowIdx, $_)
+                    }
+                }
+            }
+
+            $columnCount = $table.Columns.Count
+            for ($rowIdx = 2; $rowIdx -le $rowCount; $rowIdx++) {
+                try {
+                    $cell = $table.Cell($rowIdx, 1)
+                } catch {
+                    Write-Verbose ("Unable to access cell on slide {0} row {1}: {2}" -f $slide.SlideIndex, $rowIdx, $_)
+                    continue
+                }
+
+                if ($null -eq $cell) { continue }
+
+                $colSpan = 1
+                if ($columnCount -ge 2) {
+                    try {
+                        $colSpan += [int][object]::ReferenceEquals($cell.Shape, $table.Cell($rowIdx, 2).Shape)
+                    } catch {}
+                }
+                if ($columnCount -ge 3) {
+                    try {
+                        $colSpan += [int][object]::ReferenceEquals($cell.Shape, $table.Cell($rowIdx, 3).Shape)
+                    } catch {}
+                }
+
+                if ($colSpan -gt 1) {
+                    try {
+                        $cell.Split(1, $colSpan) | Out-Null
+                    } catch {
+                        Write-Verbose ("Unable to split monthly total span on slide {0} row {1}: {2}" -f $slide.SlideIndex, $rowIdx, $_)
+                    }
+                }
+            }
 
             $campaignStart = $null
             for ($rowIdx = 2; $rowIdx -le $rowCount; $rowIdx++) {
*** End Patch
